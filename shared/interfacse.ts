export interface User {
    imgUri: string
}

export interface Activities {
    activityType: string, 
    calories: number, 
    date: string, 
    id: number, 
    steps: number, 
    timer: number
}